
// utils/sendMessage.js

async function sendMessage(to, message) {
  // 🔁 Replace this with your real WhatsApp send logic
  console.log(`📤 Sending message to ${to}: ${message}`);
}

module.exports = { sendMessage };
